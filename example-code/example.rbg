fn foo(bar: i32) -> bool {
    while true {
        let a: f32 = 10.2 + 11.2;
	let b: String = " asa a";
        if bar == 21 {
            return true;
        } else {
            return false;
        }
    }
}

print(foo(10));

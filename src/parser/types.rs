/**
 * All types defined in Rubigo
 */
pub enum Type {
    I32,
    F32,
    Bool,
    String,
}


/**
 * All literals defined in Rubigo
 */
pub enum Literal {
    I32(i32),
    F32(f32),
    Bool(bool),
    String(String),
}
